
import DashboardWrapper from '@/components/DashboardWrapper';
import ArticlesList from '@/components/ArticlesList';

const ArticlesPage = () => {
  return (
    <DashboardWrapper title="Articles">
      <ArticlesList />
    </DashboardWrapper>
  );
};

export default ArticlesPage;
